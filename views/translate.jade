extends layout

block content
  h1= title
  p Welcome to #{title}

  form.form(name="translateForm", action="post")
    .form-group
      input(type="dropdown")
      input(type="dropdown")
        

      button(type="submit") Submit